from setuptools import setup, find_packages

package = 'depr'
version = '0.1'

setup(name=package,
      version=version,
      packages=find_packages(),
      description="Deprecation decorator",
      url='')
